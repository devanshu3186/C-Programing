//2. What data type will 3.0/8 – 2 return? 

// 3.0/8 will return a floating point number, because divide operation is performed between an integer and float, 
// Hence after subtraction from 2(which is an integer) the reuslt will be of float data type.